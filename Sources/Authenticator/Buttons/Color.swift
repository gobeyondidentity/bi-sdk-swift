import Foundation
import SharedDesign

extension Color {
    public static let primaryButtonText = Colors.background.value
    public static let primaryButtonColor = Colors.primary.value
    public static let secondaryButtonText = Colors.heading.value
    public static let secondaryButtonColor = Colors.background.value
}
